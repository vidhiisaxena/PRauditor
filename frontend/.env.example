# Base URL of the FastAPI backend.
# Local dev: point at your running uvicorn instance.
# Production: set to your deployed API (https://prauditor-backend.onrender.com).
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
